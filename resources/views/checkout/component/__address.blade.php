<div class="flex items-center ps-4 bg-primary rounded px-4 py-6">
    <p class=" ms-2">
        <span class="text-font_secondary">{{ __('page.checkout.address') }}</span> <span class="font-semibold">{{ $address ?? '-' }}</span>
    </p>
</div>